# Dependencias MINIMAS para ejecutar en local con SQLite (ideal para Windows).
# Versiones FLEXIBLES (>=) para que pip elija paquetes ya compilados (wheels)
# compatibles con tu version de Python y NO intente compilar desde codigo fuente.
# No incluye PostgreSQL (psycopg2), Redis ni librerias de ML pesadas.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
SQLAlchemy>=2.0.30
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
httpx>=0.27.0
